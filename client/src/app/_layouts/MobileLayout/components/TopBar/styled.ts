"use client";
import styled from "@emotion/styled";
import { BORDER, COLOR_WHITE } from "@/app/_theme/colors";

export const StyledTopBar = styled.aside`
  padding: 16px;
  width: 100%;
  background-color: ${COLOR_WHITE};
  border-bottom: ${BORDER};
  display: flex;
  justify-content: center;

  margin-bottom: 1rem;
`;
