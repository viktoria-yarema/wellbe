export default function AppointmentID({ params }: { params: { id: string } }) {
  return (
    <div>
      {params.id}
      <button> Fix me</button>
      Updated test id - vika rock star
    </div>
  );
}
